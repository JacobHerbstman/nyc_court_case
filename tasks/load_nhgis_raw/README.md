# load_nhgis_raw
- Does: Reads the downloaded NHGIS extract bundles and writes raw cached tabular and geometry payloads.
- Makes: Raw NHGIS parquets and load QC tables in `output/`.
- Approximate runtime: 1 to 5 minutes.

