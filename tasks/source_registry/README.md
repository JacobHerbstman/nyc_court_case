# Source Registry

Copies the version-controlled source catalog used by the download tasks.

Input: `code/source_catalog.csv`.

Output: `source_catalog.csv`, a stable copy in `output/` for downstream tasks.

The catalog lists the sources used by the production workflow.
