# load_dcp_boundaries_raw
- Does: Reads the raw DCP boundary shapefiles and writes raw cached boundary tables.
- Makes: Raw boundary parquets and load QC tables in `output/`.
- Approximate runtime: under 1 minute.

