# load_dcp_housing_database_raw
- Does: Reads the downloaded DCP Housing Database source and writes source-faithful raw caches.
- Makes: Raw housing-database parquets and load QC tables in `output/`.
- Approximate runtime: 1 to 3 minutes.

