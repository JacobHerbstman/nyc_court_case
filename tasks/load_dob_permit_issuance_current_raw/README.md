# load_dob_permit_issuance_current_raw
- Does: Reads the raw current permit issuance CSV and writes a cached raw parquet.
- Makes: A raw permit issuance parquet and load QC tables in `output/`.
- Approximate runtime: 5 to 20 minutes.

