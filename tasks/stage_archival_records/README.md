# stage_archival_records
- Does: Applies light standardization to archival return files after they have been loaded into raw caches.
- Makes: Staged archival inventories and parsed staging outputs in `output/`.
- Approximate runtime: under 1 minute for small drops; longer if the return files are large.

