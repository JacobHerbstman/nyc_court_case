# Task Graph

Reads production task Makefiles and builds a compact inventory of task outputs, upstream dependencies, and terminal production outputs.

Creates `task_flow.png`, `task_edges.csv`, `task_inventory.csv`, `task_output_inventory.csv`, and `task_graph_summary.csv`.
