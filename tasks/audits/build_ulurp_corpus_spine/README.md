# Build ULURP Corpus Spine

Builds the audit-stage universe of ZAP projects marked as ULURP, with a
project-level spine, an application-number spine, and basic coverage QC.

The source is the standardized ZAP project table from `build_zap_datasets`.
Document discovery and text extraction happen in downstream audit tasks.
