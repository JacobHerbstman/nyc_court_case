# load_census_acs_housing_raw
- Does: Reads the downloaded ACS housing files and writes raw cached tables for downstream staging.
- Makes: Raw ACS parquets and load QC tables in `output/`.
- Approximate runtime: 1 to 3 minutes.

