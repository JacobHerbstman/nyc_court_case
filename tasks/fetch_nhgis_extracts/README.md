# fetch_nhgis_extracts
- Does: Submits, waits for, and downloads NHGIS tract extracts through `ipumsr`.
- Makes: Raw NHGIS extract bundles plus extract logs and round-trip checks.
- Approximate runtime: 5 to 30 minutes, depending on NHGIS queue time.

